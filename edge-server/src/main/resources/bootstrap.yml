spring:
  application:
    name: zuul-gateway
#zuul:
#  routes:
#    manager:
#      url: http://localhost:8082
#    sales:
#      url: http://localhost:8083
server:
  port: 8080

#ribbon:
#  eureka:
#    enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
#by default eureka is used for service discovery. to disable set ribbon.eureka.enabled=false
#If ribbon doesn't use eureka for service lookups you must specify url for manager service
